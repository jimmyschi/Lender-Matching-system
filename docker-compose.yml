services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: kaaj
      POSTGRES_PASSWORD: kaaj
      POSTGRES_DB: kaaj
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
